# Utility Scripts (เครื่องมือช่วยพัฒนา)

โฟลเดอร์นี้เก็บ Script สำหรับทดสอบและ Debug ระบบ

## 📋 รายการ Scripts

| ไฟล์ | คำอธิบาย | วิธีใช้ |
|------|---------|--------|
| `check_books.js` | แสดงรายการหนังสือทั้งหมด | `node util/check_books.js` |
| `check_db.js` | แสดง Schema และตัวอย่างข้อมูล | `node util/check_db.js` |
| `debug_db_path.js` | ตรวจสอบ Path ฐานข้อมูล | `node util/debug_db_path.js` |
| `seed_dummy.js` | สร้างข้อมูล Dummy (⚠️ ใช้ตอนพัฒนาเท่านั้น) | `node util/seed_dummy.js` |
| `verify_api.js` | ทดสอบ API Endpoints | รัน Server ก่อน แล้ว `node util/verify_api.js` |

## ⚠️ หมายเหตุ
- ทุก Script ใช้ `sql.js` (ไม่ใช่ `better-sqlite3`) เพื่อรองรับ Cross-Platform
- รันจากโฟลเดอร์ `app/` เสมอ (เช่น `node util/check_books.js`)
